# ESP32 智能鱼缸控制器

## 为什么选择 ESP32

| 特性 | 说明 |
|---|---|
| 双核处理器 | Xtensa LX6 双核 240MHz，可并行处理传感器采集与控制逻辑 |
| 内置 Wi-Fi | 802.11 b/g/n，支持与手机 App 无线通信 |
| 内置蓝牙 | BLE 4.2，可用于近场配置和调试 |
| ADC | 18 路 12-bit ADC，满足多路模拟传感器（pH、TDS、溶氧等）采集需求 |
| GPIO | 34 路可用 GPIO，支持 PWM、I²C、SPI、UART、DAC |
| 低功耗模式 | Deep Sleep 电流约 10µA，适合电池备用场景 |
| 价格 | 成本低，开发生态成熟（Arduino / ESP-IDF / MicroPython） |

---

## 推荐开发板型号

| 型号 | 特点 | 适用场景 |
|---|---|---|
| ESP32-DevKitC | 标准引脚齐全，适合原型验证 | 开发调试 |
| ESP32-WROOM-32 | 模组形态，体积小 | 产品集成 |
| ESP32-S3 | 支持 USB-OTG、向量运算，适合 AI 推理 | 进阶版本 |

---

## 引脚分配规划（参考）

### 模拟传感器（ADC）

| 传感器 | 引脚 | 说明 |
|---|---|---|
| pH 探头 | GPIO34（ADC1_CH6） | 仅输入，避免启动干扰 |
| TDS 探头（水质/残饵） | GPIO35（ADC1_CH7） | 仅输入 |
| 溶氧（DO）探头 | GPIO32（ADC1_CH4） | 仅输入 |
| 水位计（模拟型） | GPIO33（ADC1_CH5） | 仅输入 |
| 环境光传感器 | GPIO36（ADC1_CH0）| VP 引脚，仅输入 |

> ⚠️ 注意：ADC2 通道在 Wi-Fi 开启时不可用，所有模拟传感器建议统一使用 **ADC1** 通道（GPIO32–GPIO39）。

### 数字传感器（I²C / OneWire / 脉冲）

| 传感器 | 协议 | 引脚 | 说明 |
|---|---|---|---|
| 水温传感器（DS18B20） | OneWire | GPIO4 | 支持多点挂载 |
| 气压 / 室温 / 湿度（BME280） | I²C | SDA=GPIO21 / SCL=GPIO22 | 标准 I²C 总线 |
| 霍尔流量计 | 脉冲中断 | GPIO25 | 硬件中断计数 |
| 点阵屏（OLED / TFT） | I²C / SPI | I²C 同上 / SPI 另分配 | 实时状态显示 |

### 执行器控制（PWM / 继电器）

| 执行器 | 引脚 | 控制方式 | 说明 |
|---|---|---|---|
| 水泵（水速调控） | GPIO26 | PWM / 继电器 | LEDC PWM 无极调速 |
| 加热棒 | GPIO27 | 继电器 | 开关控制 |
| 制冷器 | GPIO14 | 继电器 | 开关控制 |
| LED 灯（色温 / 亮度） | GPIO12, GPIO13 | PWM | 多通道 LEDC |
| 自动喂食器 | GPIO5 | 步进/舵机 PWM | 定时触发 |
| 风扇 | GPIO18 | PWM | 水面风速调控 |
| pH 调整制剂泵 | GPIO19 | 继电器 / 步进 | 精确剂量控制 |

### 报警与通知

| 功能 | 引脚 | 说明 |
|---|---|---|
| 蜂鸣器（异常报警） | GPIO23 | PWM 驱动有源蜂鸣器 |

---

## 软件架构建议

```
ESP32 固件
├── 传感器采集任务（Core 0）
│   ├── 定时采样：水温、pH、TDS、溶氧、水位、流速
│   ├── ADC 滤波（滑动平均 / 中值滤波）
│   └── 数据写入共享队列
├── 控制决策任务（Core 1）
│   ├── 读取传感器队列
│   ├── 阈值判断与报警逻辑
│   └── 驱动执行器（水泵、加热棒、灯光、喂食器）
├── 通信任务
│   ├── Wi-Fi MQTT / HTTP → 手机 App / 云端
│   └── BLE → 本地配置
└── 显示任务
    └── 点阵屏实时刷新（水温、水质健康度、运行状态）
```

---

## 关键注意事项

1. **ADC 精度**：ESP32 ADC 非线性明显，建议对 pH / TDS 等精度要求高的传感器增加外部 ADC（如 ADS1115，I²C 接口，16-bit）。
2. **电源隔离**：水体传感器（pH 探头）需与主控电路做电气隔离，防止共地干扰，可使用 ISO 隔离模块。
3. **看门狗**：启用硬件看门狗（TWDT），防止固件死锁导致执行器失控（如加热棒持续通电）。
4. **OTA 升级**：通过 Wi-Fi 实现固件 OTA 远程升级，方便后续功能迭代。
5. **断网保护**：Wi-Fi 断连时，本地控制逻辑须继续运行，不依赖云端指令。
6. **防水设计**：ESP32 模组和传感器接口板需放置于防水外壳（IP54+）中，传感器探头通过防水线缆引出。

---

## 开发工具链

| 工具 | 用途 |
|---|---|
| Arduino IDE / PlatformIO | 快速原型开发 |
| ESP-IDF | 生产级固件开发，支持 FreeRTOS 多任务 |
| MQTT Broker（如 EMQX） | 设备与 App 的消息通信 |
| Node-RED | 本地可视化调试与规则引擎 |
